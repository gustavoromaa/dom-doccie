export interface SaboresType {
    id?: number,
    categoria?: string,
    nome: string;
    descricao: string;
    precoUnitario: number;
    cor_principal: string;
    alt: string;
    imagem: string;
}